# Tinkerers Lab GECI Website (React)

Pages:
- About (also Home)
- Execom
- Gallery
- Events
- Slot Booking

## Run locally

1. Install dependencies:
   - `npm install`
2. Start dev server:
   - `npm run dev`

## Notes
- Slot booking is currently client-side only (no database/backend).
